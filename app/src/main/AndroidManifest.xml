<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/greenhouse"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/greenhouse"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArduinoConect"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SettingsGreen" />
        <activity android:name=".Graphs" />
        <activity android:name=".SplashWelcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>